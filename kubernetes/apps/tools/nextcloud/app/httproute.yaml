---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nextcloud
spec:
  parentRefs:
    - name: envoy-gateway
      namespace: network
  hostnames:
    - "nextcloud.${DOMAIN}"
  rules:
    - backendRefs:
        - name: nextcloud
          port: 8080
